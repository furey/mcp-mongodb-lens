#!/usr/bin/env node

// This file is now a wrapper around the modular implementation in the src/ directory
// It's maintained for backwards compatibility with existing scripts and installations
// For new development, consider using the modular structure directly

// Import the main entry point from the src directory
import './src/index.js'